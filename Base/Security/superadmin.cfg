name = Super Admin
users =
roles = rcon;
commands = ALL;
features = always_change_team; ban_immunity; editor; ignore_immunity; join_full; mark_any_team; mark_player; mute_immunity; name_mouseover; pingkick_immunity; silent_rcon; skip_votewait; spectator; teleport; teleport_all; teleport_target; view_collapses; view_console; view_rcon;
assign = admin; denyjoin; normal; premium; vip;